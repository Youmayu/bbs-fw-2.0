<UserControl x:Class="BBSFW.View.SystemView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BBSFW.View"
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="850">

	<UserControl.Resources>
		<BooleanToVisibilityConverter x:Key="BoolToVis" />

		<Style TargetType="{x:Type TabControl}">
			<Setter Property="Background" Value="Transparent" />
			<Setter Property="BorderThickness" Value="0" />
			<Setter Property="Template">
				<Setter.Value>
					<ControlTemplate TargetType="{x:Type TabControl}">
						<Grid KeyboardNavigation.TabNavigation="Local">
							<Grid.RowDefinitions>
								<RowDefinition Height="Auto" />
								<RowDefinition Height="*" />
							</Grid.RowDefinitions>

							<Border Grid.Row="0"
									Background="{DynamicResource SurfaceAltBrush}"
									BorderBrush="{DynamicResource BorderBrush}"
									BorderThickness="1"
									CornerRadius="8"
									Padding="4"
									Margin="0 0 0 18">
								<TabPanel x:Name="HeaderPanel"
										  IsItemsHost="True"
										  KeyboardNavigation.TabIndex="1" />
							</Border>

							<ScrollViewer Grid.Row="1"
										  VerticalScrollBarVisibility="Auto"
										  HorizontalScrollBarVisibility="Disabled">
								<ContentPresenter x:Name="PART_SelectedContentHost"
												  ContentSource="SelectedContent"
												  SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
							</ScrollViewer>
						</Grid>
					</ControlTemplate>
				</Setter.Value>
			</Setter>
		</Style>

		<Style TargetType="{x:Type TabItem}">
			<Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
			<Setter Property="FontWeight" Value="SemiBold" />
			<Setter Property="Template">
				<Setter.Value>
					<ControlTemplate TargetType="{x:Type TabItem}">
						<Border x:Name="Chrome"
								Background="Transparent"
								CornerRadius="7"
								Margin="0 0 4 0"
								Padding="13 8"
								TextElement.Foreground="{TemplateBinding Foreground}">
							<ContentPresenter ContentSource="Header"
											  HorizontalAlignment="Center"
											  VerticalAlignment="Center" />
						</Border>
						<ControlTemplate.Triggers>
							<Trigger Property="IsMouseOver" Value="True">
								<Setter TargetName="Chrome" Property="Background" Value="{DynamicResource SurfaceBrush}" />
							</Trigger>
							<Trigger Property="IsSelected" Value="True">
								<Setter Property="Foreground" Value="White" />
								<Setter TargetName="Chrome" Property="Background" Value="{DynamicResource AccentBrush}" />
							</Trigger>
						</ControlTemplate.Triggers>
					</ControlTemplate>
				</Setter.Value>
			</Setter>
		</Style>
	</UserControl.Resources>

	<TabControl>
		<TabItem Header="{DynamicResource Loc.System.Global}">
			<Grid MaxWidth="560" HorizontalAlignment="Left">
				<Grid.ColumnDefinitions>
					<ColumnDefinition Width="*" />
					<ColumnDefinition Width="10" />
					<ColumnDefinition Width="Auto" />
				</Grid.ColumnDefinitions>

				<Grid.RowDefinitions>
					<RowDefinition Height="Auto" />
					<RowDefinition Height="Auto" />
					<RowDefinition Height="Auto" />
					<RowDefinition Height="Auto" />
					<RowDefinition Height="Auto" />
					<RowDefinition Height="Auto" />
				</Grid.RowDefinitions>

				<TextBlock Grid.Row="0" Text="Global" FontSize="18" FontWeight="Bold" />

				<TextBlock Grid.Column="0" Grid.Row="1" Margin="0 10 0 0" Text="Max Current (A):" />
				<TextBox Grid.Column="2" Grid.Row="1" Margin="0 10 0 0" Width="70" HorizontalAlignment="Right" Text="{Binding ConfigVm.MaxCurrentAmps, UpdateSourceTrigger=PropertyChanged}" />

				<TextBlock Grid.Column="0" Grid.Row="2" Margin="0 10 0 0" Text="Current Ramp (A/s):" />
				<TextBox Grid.Column="2" Grid.Row="2" Margin="0 10 0 0" Width="70" HorizontalAlignment="Right" Text="{Binding ConfigVm.CurrentRampAmpsSecond, UpdateSourceTrigger=PropertyChanged}" />

				<TextBlock Grid.Column="0" Grid.Row="3" Margin="0 10 0 0" Text="Max Battery Voltage (V):" />
				<TextBox Grid.Column="2" Grid.Row="3" Margin="0 10 0 0" Width="70" HorizontalAlignment="Right" Text="{Binding ConfigVm.MaxBatteryVolts, UpdateSourceTrigger=LostFocus}" />

				<TextBlock Grid.Column="0" Grid.Row="4" Margin="0 8 0 0" Text="Low Voltage Cutoff (V):" />
				<TextBox Grid.Column="2" Grid.Row="4" Margin="0 8 0 0" Width="70" HorizontalAlignment="Right" Text="{Binding ConfigVm.LowCutoffVolts, UpdateSourceTrigger=PropertyChanged}" />

				<TextBlock Grid.Column="0" Grid.Row="5" Margin="0 10 0 0" Text="Max Speed (km/h):" Visibility="{Binding ConfigVm.UseMetricUnits, Converter={StaticResource BoolToVis}}" />
				<TextBox Grid.Column="2" Grid.Row="5" Margin="0 10 0 0" Width="70" HorizontalAlignment="Right" Text="{Binding ConfigVm.MaxSpeedKph, UpdateSourceTrigger=PropertyChanged}" Visibility="{Binding ConfigVm.UseMetricUnits, Converter={StaticResource BoolToVis}}" />

				<TextBlock Grid.Column="0" Grid.Row="5" Margin="0 10 0 0" Text="Max Speed (mph):" Visibility="{Binding ConfigVm.UseImperialUnits, Converter={StaticResource BoolToVis}}" />
				<TextBox Grid.Column="2" Grid.Row="5" Margin="0 10 0 0" Width="70" HorizontalAlignment="Right" Text="{Binding ConfigVm.MaxSpeedMph, UpdateSourceTrigger=PropertyChanged}" Visibility="{Binding ConfigVm.UseImperialUnits, Converter={StaticResource BoolToVis}}" />
			</Grid>
		</TabItem>

		<TabItem Header="{DynamicResource Loc.System.Throttle}">
			<Grid MaxWidth="620" HorizontalAlignment="Left">
				<Grid.ColumnDefinitions>
					<ColumnDefinition Width="*" />
					<ColumnDefinition Width="10" />
					<ColumnDefinition Width="Auto" />
				</Grid.ColumnDefinitions>

				<Grid.RowDefinitions>
					<RowDefinition Height="Auto" />
					<RowDefinition Height="Auto" />
					<RowDefinition Height="Auto" />
					<RowDefinition Height="Auto" />
					<RowDefinition Height="Auto" />
					<RowDefinition Height="Auto" />
				</Grid.RowDefinitions>

				<TextBlock Grid.Row="0" Text="Throttle" FontSize="18" FontWeight="Bold" />

				<TextBlock Grid.Column="0" Grid.Row="1" Margin="0 10 0 0" Text="Start Voltage (mV):" />
				<TextBox Grid.Column="2" Grid.Row="1" Margin="0 10 0 0" Width="80" HorizontalAlignment="Right" Text="{Binding ConfigVm.ThrottleStartVoltageMillivolts, UpdateSourceTrigger=PropertyChanged}" />

				<TextBlock Grid.Column="0" Grid.Row="2" Margin="0 8 0 0" Text="End Voltage (mV):" />
				<TextBox Grid.Column="2" Grid.Row="2" Margin="0 8 0 0" Width="80" HorizontalAlignment="Right" Text="{Binding ConfigVm.ThrottleEndVoltageMillivolts, UpdateSourceTrigger=PropertyChanged}" />

				<TextBlock Grid.Column="0" Grid.Row="3" Margin="0 8 0 0" Text="Start Current (%):" />
				<TextBox Grid.Column="2" Grid.Row="3" Margin="0 8 0 0" Width="80" HorizontalAlignment="Right" Text="{Binding ConfigVm.ThrottleStartCurrentPercent, UpdateSourceTrigger=PropertyChanged}" />

				<TextBlock Grid.Column="0" Grid.Row="4" Margin="0 8 0 0" Text="Global Speed Limit Options:">
					<TextBlock.ToolTip>
						<TextBlock Width="400" TextWrapping="Wrap">
						Enable global speed limit when throttle is used. Some countries have laws
						that only permit use of throttle up to a specific speed. This option can
						be used in order to comply with such laws.
						<LineBreak />
						<LineBreak />
						When this option is enabled the configured global throttle speed limit will override the 
						assist level speed limit while using throttle and not pedaling. Throttle must still
						be enabled on each individual assist level for this option to apply.
						<LineBreak />
						<LineBreak />
						<Run FontWeight="Bold">Disabled</Run> - No global speed limit.
						<LineBreak />
						<Run FontWeight="Bold">Enabled</Run> - Global speed limit applies to all assist levels.
						<LineBreak />
						<Run FontWeight="Bold">Standard Levels</Run> - Global speed limit applies to standard assist levels only.
						</TextBlock>
					</TextBlock.ToolTip>
				</TextBlock>
				<ComboBox Grid.Column="2" Grid.Row="4" Margin="0 8 0 0" Width="150" HorizontalAlignment="Right" ItemsSource="{Binding ConfigVm.ThrottleGlobalSpeedLimitOptions}" SelectedItem="{Binding ConfigVm.ThrottleGlobalSpeedLimit, UpdateSourceTrigger=PropertyChanged}" />

				<TextBlock Grid.Column="0" Grid.Row="5" Margin="0 8 0 0" Text="Global Speed Limit (%):">
					<TextBlock.ToolTip>
					Set speed limit in % of configured max speed when global throttle speed limit is enabled.
					</TextBlock.ToolTip>
				</TextBlock>
				<TextBox Grid.Column="2" Grid.Row="5" Margin="0 8 0 0" Width="80" HorizontalAlignment="Right" Text="{Binding ConfigVm.ThrottleGlobalSpeedLimitPercent, UpdateSourceTrigger=PropertyChanged}">
					<TextBox.Style>
						<Style TargetType="{x:Type TextBox}" BasedOn="{StaticResource {x:Type TextBox}}">
							<Setter Property="IsEnabled" Value="True" />
							<Style.Triggers>
								<DataTrigger Binding="{Binding ConfigVm.ThrottleGlobalSpeedLimitOpt.Value}" Value="Disabled">
									<Setter Property="IsEnabled" Value="False" />
								</DataTrigger>
							</Style.Triggers>
						</Style>
					</TextBox.Style>
				</TextBox>
			</Grid>
		</TabItem>

		<TabItem Header="{DynamicResource Loc.System.PedalAssist}">
			<Grid MaxWidth="620" HorizontalAlignment="Left">
				<Grid.ColumnDefinitions>
					<ColumnDefinition Width="*" />
					<ColumnDefinition Width="10" />
					<ColumnDefinition Width="Auto" />
				</Grid.ColumnDefinitions>

				<Grid.RowDefinitions>
					<RowDefinition Height="Auto" />
					<RowDefinition Height="Auto" />
					<RowDefinition Height="Auto" />
					<RowDefinition Height="Auto" />
					<RowDefinition Height="Auto" />
				</Grid.RowDefinitions>

				<TextBlock Grid.Row="0" Text="Pedal Assist" FontSize="18" FontWeight="Bold" />

				<TextBlock Grid.Column="0" Grid.Row="1" Margin="0 10 0 0" Text="Start Delay (degrees):" />
				<ComboBox Grid.Column="2" Grid.Row="1" Margin="0 10 0 0" Width="80" HorizontalAlignment="Right" ItemsSource="{Binding ConfigVm.PasStartDelayOptions, UpdateSourceTrigger=PropertyChanged}" SelectedItem="{Binding ConfigVm.PasStartDelayDegrees}" />

				<TextBlock Grid.Column="0" Grid.Row="2" Margin="0 8 0 0" Text="Stop Delay (ms):" />
				<TextBox Grid.Column="2" Grid.Row="2" Margin="0 8 0 0" Width="80" HorizontalAlignment="Right" Text="{Binding ConfigVm.PasStopDelayMilliseconds, UpdateSourceTrigger=PropertyChanged}" />

				<TextBlock Grid.Column="0" Grid.Row="3" Margin="0 8 0 0" Text="Keep Current (%):">
					<TextBlock.ToolTip>
						<TextBlock Width="300" TextWrapping="Wrap">
						Keep this motor current in percent of assist level target current when target cadence of assist level has been reached.
						The assist level current will start ramp down towards this value when cadence hits the limit configured in 'Keep Current Cadence (rpm)'.
						</TextBlock>
					</TextBlock.ToolTip>
				</TextBlock>
				<TextBox Grid.Column="2" Grid.Row="3" Margin="0 8 0 0" Width="80" HorizontalAlignment="Right" Text="{Binding ConfigVm.PasKeepCurrentPercent, UpdateSourceTrigger=PropertyChanged}" />

				<TextBlock Grid.Column="0" Grid.Row="4" Margin="0 8 0 0" Text="Keep Current Cadence (rpm):">
					<TextBlock.ToolTip>
						<TextBlock Width="300" TextWrapping="Wrap">
						Lower cadence limit for when 'Keep Current %' ramp starts, configured in rpm.
						When cadence gets above this value the motor current will decrease towards the current calculated using configured value of 'Keep Current %'.
						</TextBlock>
					</TextBlock.ToolTip>
				</TextBlock>
				<TextBox Grid.Column="2" Grid.Row="4" Margin="0 8 0 0" Width="80" HorizontalAlignment="Right" Text="{Binding ConfigVm.PasKeepCurrentCadenceRpm, UpdateSourceTrigger=PropertyChanged}" />
			</Grid>
		</TabItem>

		<TabItem Header="{DynamicResource Loc.System.Features}">
			<Grid MaxWidth="680" HorizontalAlignment="Left">
				<Grid.ColumnDefinitions>
					<ColumnDefinition Width="*" />
					<ColumnDefinition Width="10" />
					<ColumnDefinition Width="Auto" />
				</Grid.ColumnDefinitions>

				<Grid.RowDefinitions>
					<RowDefinition Height="Auto" />
					<RowDefinition Height="Auto" />
					<RowDefinition Height="Auto" />
					<RowDefinition Height="Auto" />
					<RowDefinition Height="Auto" />
					<RowDefinition Height="Auto" />
					<RowDefinition Height="Auto" />
					<RowDefinition Height="Auto" />
				</Grid.RowDefinitions>

				<TextBlock Grid.Row="0" Text="Features" FontSize="18" FontWeight="Bold" />

				<TextBlock Grid.Column="0" Grid.Row="1" Margin="0 8 0 0" Text="Speed Sensor:" />
				<CheckBox Grid.Column="2" Grid.Row="1" Margin="0 8 0 0" HorizontalAlignment="Right" IsChecked="{Binding ConfigVm.UseSpeedSensor, UpdateSourceTrigger=PropertyChanged}" />

				<TextBlock Grid.Column="0" Grid.Row="2" Margin="0 8 0 0" Text="Shift Sensor:" />
				<CheckBox Grid.Column="2" Grid.Row="2" Margin="0 8 0 0" HorizontalAlignment="Right" IsChecked="{Binding ConfigVm.UseShiftSensor, UpdateSourceTrigger=PropertyChanged}" IsEnabled="{Binding ConfigVm.IsShiftSensorSupported}" />

				<TextBlock Grid.Column="0" Grid.Row="3" Margin="0 8 0 0" Text="Walk Mode:" />
				<CheckBox Grid.Column="2" Grid.Row="3" Margin="0 8 0 0" HorizontalAlignment="Right" IsChecked="{Binding ConfigVm.UsePushWalk, UpdateSourceTrigger=PropertyChanged}" />

				<TextBlock Grid.Column="0" Grid.Row="4" Margin="0 8 0 0" Text="Temperature Sensor:">
					<TextBlock.ToolTip>
						<TextBlock Width="300" TextWrapping="Wrap">
						Select which temperature sensors to use for thermal limiting if motor/controller gets too hot.
						Normally you should have this set to "All", but if you have a motor or controller with a broken
						temperature sensor it can be useful to select only the sensors that are working.
						</TextBlock>
					</TextBlock.ToolTip>
				</TextBlock>
				<ComboBox Grid.Column="2" Grid.Row="4" Margin="0 8 0 0" Width="120" HorizontalAlignment="Right" ItemsSource="{Binding ConfigVm.TemperatureSensorOptions}" SelectedItem="{Binding ConfigVm.UseTemperatureSensor, UpdateSourceTrigger=PropertyChanged}" />

				<TextBlock Grid.Column="0" Grid.Row="5" Margin="0 8 0 0" Text="Lights Mode:">
					<TextBlock.ToolTip>
						<TextBlock Width="400" TextWrapping="Wrap">
						Options for controlling external lights output.
						<LineBreak />
						<LineBreak />
						<Run FontWeight="Bold">Default</Run> - Controlled by display.
						<LineBreak />
						<Run FontWeight="Bold">Disabled</Run> - Lights output disabled.
						<LineBreak />
						<Run FontWeight="Bold">Always On</Run> - Lights output always on.
						<LineBreak />
						<Run FontWeight="Bold">Brake Light</Run> - Lights output enabled while braking.
						</TextBlock>
					</TextBlock.ToolTip>
				</TextBlock>
				<ComboBox Grid.Column="2" Grid.Row="5" Margin="0 8 0 0" Width="120" HorizontalAlignment="Right" ItemsSource="{Binding ConfigVm.LightsModeOptions}" SelectedItem="{Binding ConfigVm.LightsMode, UpdateSourceTrigger=PropertyChanged}" />

				<TextBlock Grid.Column="0" Grid.Row="6" Margin="0 8 0 0" Text="Pretension Chain:">
					<TextBlock.ToolTip>
						<TextBlock Width="300" TextWrapping="Wrap">
							Apply 1% of Max Current when above a threshold road speed to pre-tension the drivetrain.
							This makes it nicer for more aggressive PAS / throttle settings since freewheel pawls
							will not get smacked if there is play in the drivetrain system.
						</TextBlock>
					</TextBlock.ToolTip>
				</TextBlock>
				<CheckBox Grid.Column="2" Grid.Row="6" Margin="0 8 0 0" HorizontalAlignment="Right" IsChecked="{Binding ConfigVm.UsePretension, UpdateSourceTrigger=PropertyChanged}" IsEnabled="{Binding ConfigVm.UseSpeedSensor}" />

				<TextBlock Grid.Column="0" Grid.Row="7" Margin="0 10 0 0" Text="Pretension Speed Cutoff (km/h):" Visibility="{Binding ConfigVm.UseMetricUnits, Converter={StaticResource BoolToVis}}">
					<TextBlock.ToolTip>
						<TextBlock Width="300" TextWrapping="Wrap">
							Pretension will not be applied at and below this road speed. Take caution setting this to zero, as
							your bike will roll forward as soon as it is powered on and pushed. Recommended to start with about 16 kph
							and lower if necessary once comfortable with the feature.
						</TextBlock>
					</TextBlock.ToolTip>
				</TextBlock>
				<TextBox Grid.Column="2" Grid.Row="7" Margin="0 10 0 0" Width="80" HorizontalAlignment="Right" Text="{Binding ConfigVm.PretensionSpeedCutoffKph, UpdateSourceTrigger=PropertyChanged}" Visibility="{Binding ConfigVm.UseMetricUnits, Converter={StaticResource BoolToVis}}" IsEnabled="{Binding ConfigVm.UseSpeedSensor}" />

				<TextBlock Grid.Column="0" Grid.Row="7" Margin="0 10 0 0" Text="Pretension Speed Cutoff (mph):" Visibility="{Binding ConfigVm.UseImperialUnits, Converter={StaticResource BoolToVis}}">
					<TextBlock.ToolTip>
						<TextBlock Width="300" TextWrapping="Wrap">
							Pretension will not be applied at and below this road speed. Take caution setting this to zero, as
							your bike will roll forward as soon as it is powered on and pushed. Recommended to start with about 10 mph
							and lower if necessary once comfortable with the feature.
						</TextBlock>
					</TextBlock.ToolTip>
				</TextBlock>
				<TextBox Grid.Column="2" Grid.Row="7" Margin="0 10 0 0" Width="80" HorizontalAlignment="Right" Text="{Binding ConfigVm.PretensionSpeedCutoffMph, UpdateSourceTrigger=PropertyChanged}" Visibility="{Binding ConfigVm.UseImperialUnits, Converter={StaticResource BoolToVis}}" IsEnabled="{Binding ConfigVm.UseSpeedSensor}" />
			</Grid>
		</TabItem>

		<TabItem Header="{DynamicResource Loc.System.SpeedShift}">
			<StackPanel MaxWidth="620" HorizontalAlignment="Left">
				<Grid>
					<Grid.ColumnDefinitions>
						<ColumnDefinition Width="*" />
						<ColumnDefinition Width="10" />
						<ColumnDefinition Width="Auto" />
					</Grid.ColumnDefinitions>

					<Grid.RowDefinitions>
						<RowDefinition Height="Auto" />
						<RowDefinition Height="Auto" />
						<RowDefinition Height="Auto" />
					</Grid.RowDefinitions>

					<TextBlock Grid.Row="0" Text="Speed Sensor" FontSize="18" FontWeight="Bold" />

					<TextBlock Grid.Column="0" Grid.Row="1" Margin="0 10 0 0" Text="Wheel Size (inch):" />
					<TextBox Grid.Column="2" Grid.Row="1" Margin="0 10 0 0" Width="80" HorizontalAlignment="Right" Text="{Binding ConfigVm.WheelSizeInch, UpdateSourceTrigger=LostFocus}" />

					<TextBlock Grid.Column="0" Grid.Row="2" Margin="0 8 0 0" Text="Signals (per rotation):" />
					<TextBox Grid.Column="2" Grid.Row="2" Margin="0 8 0 0" Width="80" HorizontalAlignment="Right" Text="{Binding ConfigVm.SpeedSensorSignals, UpdateSourceTrigger=PropertyChanged}" />
				</Grid>

				<Grid Margin="0 24 0 0">
					<Grid.ColumnDefinitions>
						<ColumnDefinition Width="*" />
						<ColumnDefinition Width="10" />
						<ColumnDefinition Width="Auto" />
					</Grid.ColumnDefinitions>

					<Grid.RowDefinitions>
						<RowDefinition Height="Auto" />
						<RowDefinition Height="Auto" />
						<RowDefinition Height="Auto" />
					</Grid.RowDefinitions>

					<TextBlock Grid.Row="0" Text="Shift Sensor" FontSize="18" FontWeight="Bold" />

					<TextBlock Grid.Column="0" Grid.Row="1" Margin="0 8 0 0" Text="Shift Interrupt Duration (ms):">
						<TextBlock.ToolTip>
							<TextBlock Width="300" TextWrapping="Wrap">
							Duration of the power interrupt when the shift sensor is triggered.
							</TextBlock>
						</TextBlock.ToolTip>
					</TextBlock>
					<TextBox Grid.Column="2" Grid.Row="1" Margin="0 8 0 0" Width="80" HorizontalAlignment="Right"
							 IsEnabled="{Binding ConfigVm.IsShiftSensorSupported}"
							 Text="{Binding ConfigVm.ShiftInterruptDuration, UpdateSourceTrigger=PropertyChanged}" />

					<TextBlock Grid.Column="0" Grid.Row="2" Margin="0 8 0 0" Text="Shift Current Threshold (%):">
						<TextBlock.ToolTip>
							<TextBlock Width="300" TextWrapping="Wrap">
							Maximum motor current during shifting expressed as a percentage of "Max Current (A)".
							If motor current is below this threshold, then no power cut will happen.
							If motor current is higher than this threshold, then current will be reduced to this value during the shift.
							<LineBreak />
							For example, if Max Current is 30 amps, and Shift Current Threshold is 10%, then current through the motor during
							shifts will be limited to 3 amps.
							</TextBlock>
						</TextBlock.ToolTip>
					</TextBlock>
					<TextBox Grid.Column="2" Grid.Row="2" Margin="0 8 0 0" Width="80" HorizontalAlignment="Right"
							 IsEnabled="{Binding ConfigVm.IsShiftSensorSupported}"
							 Text="{Binding ConfigVm.ShiftInterruptCurrentThresholdPercent, UpdateSourceTrigger=PropertyChanged}" />
				</Grid>
			</StackPanel>
		</TabItem>

		<TabItem Header="{DynamicResource Loc.System.Display}">
			<Grid MaxWidth="680" HorizontalAlignment="Left">
				<Grid.ColumnDefinitions>
					<ColumnDefinition Width="*" />
					<ColumnDefinition Width="10" />
					<ColumnDefinition Width="Auto" />
				</Grid.ColumnDefinitions>

				<Grid.RowDefinitions>
					<RowDefinition Height="Auto" />
					<RowDefinition Height="Auto" />
					<RowDefinition Height="Auto" />
				</Grid.RowDefinitions>

				<TextBlock Grid.Row="0" Text="Display" FontSize="18" FontWeight="Bold" />

				<TextBlock Grid.Column="0" Grid.Row="1" Margin="0 10 0 0" Text="Walk Mode Data Display:">
					<TextBlock.ToolTip>
						<TextBlock Width="400" TextWrapping="Wrap">
						Select what data to show in the speed field on the display. Walk mode is usually engaged by long press on the minus button of the display.
						<LineBreak />
						<LineBreak />
						<Run FontWeight="Bold">Speed</Run> - Default setting, continue to display road speed.
						<LineBreak />
						<Run FontWeight="Bold">Temperature</Run> - Display max value of motor/controller temperature in deg C.
						<LineBreak />
						<Run FontWeight="Bold">Requested Power</Run> - Display requested motor power in percent.
						<LineBreak />
						<Run FontWeight="Bold">Battery Level</Run> - Display computed battery level in percent.
						</TextBlock>
					</TextBlock.ToolTip>
				</TextBlock>
				<ComboBox Grid.Column="2" Grid.Row="1" Margin="0 8 0 0" Width="150" HorizontalAlignment="Right" ItemsSource="{Binding ConfigVm.WalkModeDataDisplayOptions}" SelectedItem="{Binding ConfigVm.WalkModeDataDisplay, UpdateSourceTrigger=PropertyChanged}" />

				<TextBlock Grid.Column="0" Grid.Row="2" Margin="0 8 0 0" Text="Display:">
					<TextBlock.ToolTip>
						<TextBlock Width="400" TextWrapping="Wrap">
						Select the connected display type.
						<LineBreak />
						<LineBreak />
						<Run FontWeight="Bold">Other displays</Run> - Uses standard Bafang display work-mode handling.
						<LineBreak />
						<Run FontWeight="Bold">860C</Run> - Ignores display work-mode requests so the controller controls ECO/Sport mode.
						</TextBlock>
					</TextBlock.ToolTip>
				</TextBlock>
				<ComboBox Grid.Column="2" Grid.Row="2" Margin="0 8 0 0" Width="150" HorizontalAlignment="Right" ItemsSource="{Binding ConfigVm.DisplayTypeOptions}" SelectedItem="{Binding ConfigVm.Display, UpdateSourceTrigger=PropertyChanged}" />
			</Grid>
		</TabItem>
	</TabControl>
</UserControl>
